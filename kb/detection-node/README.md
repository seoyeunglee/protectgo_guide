# 탐지 시나리오·노드

Protect Go의 탐지는 코드나 설정 파일이 아니라 **노드를 연결한 그림**으로 정의한다. 탐지 시나리오는 "어떤 데이터를, 어떤 기준으로 감시하고, 이상이 생기면 누구에게 알릴지"를 하나의 흐름으로 묶은 단위다 — 시나리오 하나가 캔버스 한 장이고, 캔버스 위의 노드들이 그 흐름을 구성한다.

## 데이터가 알림이 되기까지

노드는 항상 왼쪽에서 오른쪽으로 흐른다. 입력에서 시작해 판정과 조건을 거쳐 출력으로 끝난다.

1. **데이터 노드**가 센서·카메라의 측정값과 영상을 받아들인다.
2. **탐지 설정 노드**(또는 AI 패키지 노드)가 그 데이터를 기준과 비교해 이상 여부를 판정한다.
3. **이벤트 설정 노드**가 "기준값을 몇 번 넘으면 이벤트로 볼 것인지" 발생 조건을 정의한다.
4. **알림 및 제어 노드**가 발생한 이벤트를 대시보드 푸시, 모바일 푸시, SOP 등으로 전달한다.

영상 탐지에는 관심 영역을 지정하는 ROI 노드가 추가로 쓰인다.

## 탐지 시나리오 5종

감시하려는 데이터에 따라 시나리오 유형이 정해진다.

| 유형 | 데이터 소스 | 판정 방식 |
|---|---|---|
| 영상 이상 탐지 | CCTV·열화상 영상 | 비전 AI 모델 (작업자 안전 모델 등) |
| DB 임계치 탐지 | 외부 DB | 수치 임계치 비교 |
| 센서 임계치 탐지 | 진동·온도·복합가스·열화상 센서 | 수치 임계치 비교 |
| 통계 분석 탐지 | 센서(다중) | Z-Score / 변화율 / EWMA 통계 이상치 |
| 발전 효율 저하 탐지 | 전력 센서 + 발전량 데이터 | 발전량 예측값 대비 실측값 비교 |

## 왜 노드 방식인가

- **흐름이 보인다**: 데이터부터 알림까지 한 화면에서 추적할 수 있어, 알림이 왜 왔는지 연결을 따라가며 확인할 수 있다.
- **조합이 자유롭다**: 같은 데이터 노드를 여러 탐지 설정에 연결하거나, 여러 데이터를 하나의 이벤트로 묶는(융합 이벤트) 구성을 그림으로 만들 수 있다.
- **검증이 내장된다**: 연결할 수 없는 노드 조합은 연결 시점에 차단되고 안내 팝업이 표시된다.

## Diátaxis 4유형
- [tutorial/](./tutorial/) — 학습 지향: 첫 탐지 시나리오를 처음부터 끝까지 만들어보는 안내
- [how-to/](./how-to/) — 작업 지향: 특정 노드를 구성·연결하는 절차
- [reference/](./reference/) — 정보 지향: 노드 종류·속성·제약값 사양
- [explanation/](./explanation/) — 이해 지향: 노드 시스템의 개념·동작 원리

> 도메인 개념 설명(구 `explanation/scenario-node-concepts`)은 2026-06-12 IA 개편으로 이 랜딩 문서에 병합했다.
> 출발점: `protect-go-knowledge/04-detection-nodes.md`, `05-detection-scenarios.md` + 화면설계서 PDF 정책 추출분 + 프론트엔드 코드. 스키마는 [schema/kb-entry.md](../../schema/kb-entry.md) 기준.
